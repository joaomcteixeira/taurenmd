# appveyor.yml
---
environment:
  matrix:
  - TOXENV: py35
  - TOXENV: py36
  - TOXENV: py37

build: off

install:
- pip install tox tox-conda
- python -m pip install --upgrade pip

test_script:
- tox

